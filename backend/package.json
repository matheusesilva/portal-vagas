{
  "name": "portal-vagas-backend",
  "version": "1.0.0",
  "scripts": {
    "deploy:save-preferences": "node ../scripts/deploy-lambda.js save-user-preferences ./lambdas/save-user-preferences/function.zip",
    "deploy:get-preferences": "node ../scripts/deploy-lambda.js get-user-preferences ./lambdas/get-user-preferences/function.zip",
    "deploy:all": "npm run deploy:save-preferences && npm run deploy:get-preferences"
  },
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.450.0"
  }
}
